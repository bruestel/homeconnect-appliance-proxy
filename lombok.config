# tell Lombok that this is the root directory and that it shouldn’t search parent directories for more configuration files
config.stopBubbling = true
# Add @Generated to indicate JaCoCo + 0.8.0 release that code is not to include for test coverage
lombok.addLombokGeneratedAnnotation = true

lombok.anyConstructor.addConstructorProperties=true
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
